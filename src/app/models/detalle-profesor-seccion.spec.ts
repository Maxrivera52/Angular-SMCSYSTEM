import { DetalleProfesorSeccion } from './detalle-profesor-seccion';

describe('DetalleProfesorSeccion', () => {
  it('should create an instance', () => {
    expect(new DetalleProfesorSeccion()).toBeTruthy();
  });
});
