import { DetalleCursoDocente } from './detalle-curso-docente';

describe('DetalleCursoDocente', () => {
  it('should create an instance', () => {
    expect(new DetalleCursoDocente()).toBeTruthy();
  });
});
